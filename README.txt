ملفّات التطبيق:
- index.html (الواجهة الرئيسية)
- manifest.webmanifest
- sw.js (للعمل دون اتصال بعد أول تشغيل عبر https)
- icon.svg

تشغيل سريع:
1) ضع الملفات في مجلد واحد.
2) افتحها عبر خادم محلي أو GitHub Pages (مهم للتثبيت و Service Worker).
3) داخل التطبيق: استيراد Excel ثم البحث عن الطالب ثم إنشاء سجلات وإرسال واتساب.
4) نقل البيانات: تصدير JSON ثم استيراده في جهاز آخر.

ملاحظة:
فتح index.html مباشرةً من file:// يعمل لمعظم الميزات، لكن التثبيت والعمل دون اتصال يحتاج https.
